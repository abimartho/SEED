// Test2: Move forward in a straight line and stop after a specified distance in feet (between 1 and 10 feet).
// Test3: Rotate a specified number of radians then move forward a certain distance

// Define the distance from the center to a wheel in inches
#define R 5.5
int inchesToCounts(int inches); // Would we get more accuracy for inches and degrees if using double.
double feetToCounts(double feet);
double radsToCounts(double rads);
int degreesToCounts(double degree);
